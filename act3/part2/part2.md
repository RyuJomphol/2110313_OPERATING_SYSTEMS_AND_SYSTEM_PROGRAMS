# Activity 3.2: การใช้ signal() และ alarm()

กิจกรรมนี้มีวัตถุประสงค์เพื่อให้น้อง ๆ เข้าใจการทำงานของ **signal** และ **timer (alarm)** ในระบบปฏิบัติการ Unix/Linux โดยโปรแกรมจะทำงานสลับระหว่าง

* การคำนวณ (computing)
* การหยุดทำงานชั่วคราว (sleep)

และปรับปรุงจากโจทย์เดิม โดยเปลี่ยนจากการรับค่าผ่าน **argument ของ command line** มาเป็นการรับค่าจาก **แป้นพิมพ์ (keyboard input)**

---

## 1. อธิบายคำสั่งที่ใช้

### 1.1 `signal()`

`signal()` ใช้สำหรับกำหนด **signal handler** หรือฟังก์ชันที่ระบบจะเรียกอัตโนมัติเมื่อโปรแกรมได้รับสัญญาณ (signal) ที่กำหนด

รูปแบบการใช้งาน:

```c
signal(signal_type, handler_function);
```

ในโปรแกรมนี้:

```c
signal(SIGALRM, on_alarm);
```

หมายความว่า:

* เมื่อโปรแกรมได้รับสัญญาณ `SIGALRM`
* ระบบจะเรียกฟังก์ชัน `on_alarm()` แทนการทำงานปกติ

---

### 1.2 `alarm()`

`alarm()` ใช้สำหรับตั้งเวลา (เป็นวินาที) เพื่อส่งสัญญาณ `SIGALRM` ไปยังโปรเซส

รูปแบบการใช้งาน:

```c
alarm(seconds);
```

การทำงาน:

* เมื่อเวลาที่กำหนดครบ ระบบจะส่ง `SIGALRM`
* หากมีการตั้ง `signal(SIGALRM, ...)` ไว้ โปรแกรมจะถูกขัดจังหวะและเรียก handler ทันที

ในโปรแกรมนี้ `alarm()` ถูกเรียกซ้ำเพื่อให้เกิดการทำงานเป็นรอบ ๆ อย่างต่อเนื่อง

---

## 2. อธิบายการทำงานของโค้ด

### 2.1 การประกาศตัวแปรและ library

```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <signal.h>

int compute_period = 5;
int sleep_period = 5;
```

* ใช้ `signal.h` สำหรับจัดการ signal
* ใช้ `unistd.h` สำหรับ `sleep()` และ `alarm()`
* ตัวแปร `compute_period` และ `sleep_period` ใช้เก็บช่วงเวลาการทำงาน

---

### 2.2 ฟังก์ชัน `on_alarm()` (Signal Handler)

```c
void on_alarm (int signal) {
    printf("Sleep\n");
    sleep(sleep_period);
    printf("Wake up\n");

    alarm(compute_period);
}
```

ฟังก์ชันนี้จะถูกเรียกเมื่อเกิด `SIGALRM`

ลำดับการทำงาน:

1. แสดงข้อความ `Sleep`
2. หยุดการทำงานตามค่า `sleep_period`
3. แสดงข้อความ `Wake up`
4. ตั้ง `alarm()` ใหม่ เพื่อให้เกิดรอบถัดไป

---

### 2.3 การรับค่าจากแป้นพิมพ์ (ส่วนที่แก้จากโจทย์เดิม)

```c
printf("Enter compute period :\n");
scanf("%d", &compute_period);

printf("Enter sleep period :\n");
scanf("%d", &sleep_period);
```

**จุดที่แก้ไขจากโจทย์เดิม**:

* เดิมใช้ `argc` และ `argv` เพื่อรับค่าจาก command line
* เวอร์ชันนี้เปลี่ยนเป็นใช้ `scanf()` รับค่าจากผู้ใช้โดยตรง
* ทำให้ใช้งานง่ายและตรงกับตัวอย่าง output ที่กำหนด

---

### 2.4 การผูก signal และตั้งเวลา

```c
signal(SIGALRM, on_alarm);
alarm(compute_period);
```

* กำหนดให้ `SIGALRM` เรียกฟังก์ชัน `on_alarm()`
* ตั้งเวลาเริ่มต้นก่อนเข้าสู่ลูปคำนวณ

---

### 2.5 ลูปการคำนวณแบบไม่สิ้นสุด

```c
for (i = 0; ; i++) {
    if (i == 0) printf("computing\n");
}
```

* เป็นลูปที่ทำงานไม่รู้จบ (infinite loop)
* แสดงคำว่า `computing` เพียงครั้งเดียว
* การทำงานของลูปจะถูกขัดจังหวะเป็นระยะ ๆ โดย `SIGALRM`

---

## 3. สรุปภาพรวมการทำงานของโปรแกรม

ลำดับการทำงาน:

1. รับค่า `compute_period` และ `sleep_period` จากแป้นพิมพ์
2. โปรแกรมเริ่มคำนวณ (`computing`)
3. เมื่อครบ `compute_period` วินาที จะเกิด `SIGALRM`
4. โปรแกรมเข้าสู่โหมด `Sleep`
5. หลังครบ `sleep_period` วินาที จะ `Wake up`
6. วนซ้ำกระบวนการเดิมไปเรื่อย ๆ

โปรแกรมนี้ช่วยให้น้อง ๆ เข้าใจแนวคิด **signal-driven execution** และการขัดจังหวะการทำงานของโปรเซสในระบบปฏิบัติการได้อย่างชัดเจน 🚀
